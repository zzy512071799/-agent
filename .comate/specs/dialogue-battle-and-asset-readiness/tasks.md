# 漫剧视频 Prompt、参考图模式与资产完成度任务计划

- [x] Task 1: 固化 Wan3 Prompt 数据结构
    - 1.1: 保留 `wan3Prompt`、`wan3Mode`、首帧、尾帧、参考图、音频参考和继承字段
    - 1.2: 保留镜段参考图的文件名、用途、顺序和存在性
    - 1.3: 确认 `Shot.parse()`、复用和导出流程不会丢失 Wan3 扩展字段

- [x] Task 2: 按最终 Prompt 格式编译视频提示词
    - 已将 ep01 中已有人工 `wan3Prompt` 同步迁移到最终格式，避免仅修改自动编译器而不影响现有镜头文本
    - 已将参考图锚点内联到人物/场景/道具的画面动作处，并将对白按动作顺序写入同步时序
    - 2.1: 先输出“首帧”或“首帧/尾帧”锁定约束
    - 2.2: 写清开场/结束构图、空间关系、人物姿态、道具形态和全片生效条件
    - 2.3: 对每个角色、场景、道具使用 `@图片` 标注，并说明该图片锁定的身份、外形、纹理、比例或空间关系
    - 2.4: 输出“生成单镜头。无台词。”或完整对白，不虚构台词
    - 2.5: 集中输出“镜头参数”，包含景别、视角、机位、焦段、运镜、速度、幅度和手持状态
    - 2.6: 以“画面内容”描述连续因果链：承接前序状态、触发动作、运动轨迹、接触/受力、人物反应和环境变化
    - 2.7: 输出明确“结束状态”，说明人物、道具、空间和情绪最终停在哪里
    - 2.8: 默认禁止“第几秒到第几秒”的硬拆分；只有固定节拍、音画同步或明确转场才允许使用时间点
    - 2.9: 禁止使用“发生战斗”“人物受伤”“镜头推进”等空泛概括，必须写明对象、方向、速度、接触点、比例和状态变化
    - 2.10: 已有人工定稿 Prompt 默认保留；缺失或明确要求重编译时才生成新 Prompt

- [x] Task 3: 生成 Prompt 中的图片引用清单
    - 3.1: 从角色、场景、道具和显式 `wan3ReferencePaths` 构建有序参考图清单
    - 3.2: 保持 Prompt 中 `@图片1`、`@图片2` 与 API 实际上传顺序一致
    - 3.3: 首帧/尾帧引用明确写出锁定内容和不可改变的空间关系
    - 3.4: 缺失、格式错误或内容/命名不一致的图片不得作为有效参考图上传
    - 3.5: `/api/segments` 返回引用用途、顺序、文件名和存在性

- [x] Task 4: 选择并执行镜段生成模式
    - 4.1: 构图和关键视觉信息必须锁死的镜段使用首帧模式
    - 4.2: 需要连续变化、角色/场景/道具多图约束且不依赖单一构图的镜段使用全能参考模式
    - 4.3: 需要承接上一段真实结束状态的镜段使用上一段尾帧继承的首帧链
    - 4.4: 只有存在真实有效 `lastFramePath` 时才使用首尾帧模式
    - 4.5: 首帧模式禁止混入 `reference_image`
    - 4.6: 全能参考模式禁止同时带 `firstFramePath`，并校验最多 10 张参考图
    - 4.7: 当前《人间渡》ep01 分配：S01/S02/S04/S05/S06/S07 使用首帧，S03 使用全能参考，S05 继承 S04 实际尾帧

- [ ] Task 5: 校验和修复图片资产
    - 5.1: 核验角色、场景、道具图片是否存在、格式有效且内容与命名一致
    - 5.2: 复核 `阿离-正面.png`，必须确认实际为正面后才能通过
    - 5.3: 生成并校验缺失的 `妇人-正面.png`
    - 5.4: 生成并校验缺失的 `漏风书房.png`
    - 5.5: 生成失败保留原图、备份和失败日志，不覆盖错误资产
    - 5.6: OneAPI 上游持续失败时保持 `incomplete`，不得伪造 `ready`

- [x] Task 6: 更新前端和接口核对展示
    - 6.1: 展示首帧/尾帧锁定内容、`@图片` 引用和实际上传顺序
    - 6.2: 区分首帧、全能参考、视觉参考和道具/构图素材
    - 6.3: 展示缺失关键帧、缺失资产和命名/内容不一致原因
    - 6.4: 展示 Prompt 是否包含连续动作和结束状态
    - 6.5: 不把“接口任务创建成功”直接当成“图片或视频内容合格”

- [ ] Task 7: 执行回归验证
    - 7.1: 验证 S03 的全能参考图顺序与 Prompt 中 `@图片1/@图片2` 一致
    - 7.2: 验证 S05 使用 S04 实际尾帧继承，未伪造尾帧路径
    - 7.3: 验证首帧模式不会上传额外参考图
    - 7.4: 验证全能参考模式不会上传首帧
    - 7.5: 验证对白、战斗动作链、镜头参数和结束状态都保留
    - 7.6: 验证默认 Prompt 不含不必要的秒级拆分
    - 7.7: 运行 `npm run typecheck`、JSON 校验、接口回归和图片格式检查
    - 7.8: 只有所有必需资产和引用图片有效后才标记整体 `ready`

- [x] Task 8: 生成实施总结
    - 8.1: 记录最终 Prompt 格式和示例
    - 8.2: 记录各镜段模式选择和参考图清单
    - 8.3: 记录资产校验、生成成功/失败和剩余阻塞项
    - 8.4: 将结果写入 `.comate/specs/dialogue-battle-and-asset-readiness/summary.md`
