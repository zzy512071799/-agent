# Wan3 EP01 Prompt 审计与修正任务计划

- [✓] Task 1: 建立统一的镜头媒体引用模型
    - 1.1: 读取当前 schema、segments、produce 和前端数据结构，确认现有字段可复用范围
    - 1.2: 设计并实现按稳定顺序生成镜头媒体依赖的内部逻辑
    - 1.3: 区分 first_frame、last_frame、reference_image、visual_reference 和 prop 的输入角色
    - 1.4: 为每个依赖计算 index、token、路径、存在性、可上传性、问题和预览信息
    - 1.5: 为缺失、重复、mismatch 和引用越界建立可判定的阻断结果

- [x] Task 2: 补齐并校正 EP01 全能参考图片资产
    - 2.1: 核验 S01-S07 当前出场人物、场景和动作道具的真实图片资产
    - 2.2: 处理阿离正面图文件名与实际内容不一致的问题，不覆盖原图
    - 2.3: 补齐妇人角色图、漏风书房场景图及其他缺失的角色/场景/道具图
    - 2.4: 对新增或替换图片执行格式、尺寸、可读性和内容一致性复核
    - 2.5: 核验每镜参考图数量不超过 10 张，删除无关或重复参考图
    - 2.6: 同步分镜表中的资产意图和缺失状态，禁止将失败生成标记为 ready

- [x] Task 3: 将 S01-S07 改为全能参考 Prompt
    - 3.1: 将 S01-S07 的 wan3Mode 统一调整为 reference，移除互斥的 firstFramePath 上传语义
    - 3.2: 按“前镜真实尾帧、出场人物、当前场景、关键道具、必要构图参考”排序并编号 @图片N
    - 3.3: 将每个 @图片N 内联到对应人物出现、动作发生、道具接触或场景约束节点
    - 3.4: 在 Prompt 开头明确 @图片1 是否为前镜尾帧，以及它锁定的开场承接状态
    - 3.5: 为每个镜头补齐动作因果链、对白发声时序和具体结束状态
    - 3.6: 删除无必要的第几秒到第几秒硬编码拆分，保留必要的镜头节奏和参数
    - 3.7: 确认每个 Prompt 的引用编号、参考图顺序、模式和实际资产声明一致

- [x] Task 4: 接通全能参考的 segments 引用元数据和预览地址
    - 4.1: 让 /api/segments 从统一媒体依赖生成 Prompt 引用元数据
    - 4.2: 返回前镜尾帧、人物、场景和道具的稳定 token、媒体顺序、存在性、可上传性、问题和 previewUrl
    - 4.3: 在 reference 模式下不再把角色、场景和道具降级为仅供核对的 visual_reference
    - 4.4: 展示前镜尾帧缺失、mismatch、超出 10 张和引用越界等具体状态
    - 4.5: 增加接口测试覆盖连续尾帧顺序、缺失资源和引用越界

- [x] Task 5: 统一全能参考的 produce media 映射与镜头衔接
    - 5.1: 让 /api/produce 复用同一份有序媒体依赖，不再通过文件名猜测 Prompt 引用位置
    - 5.2: 对 reference 模式按 @图片N 顺序传递 reference_image，确保 media[N-1] 指向同一图片
    - 5.3: 将前一镜真实尾帧作为下一镜参考列表第一项，并写入连续性约束
    - 5.4: 前一镜没有真实尾帧时阻断下一镜，不退回虚假的设计首帧
    - 5.5: 保持每镜 reference_image 不超过 10 张，阻断缺失、mismatch 和不可上传资源
    - 5.6: 增加 produce 编排测试，确认模式、顺序和衔接阻断正确

- [x] Task 6: 实现 Prompt 引用到网页图片卡片的交互绑定
    - 6.1: 读取当前 Prompt 展示组件和 ImageCard 行为，确定最小改动方案
    - 6.2: 将 @图片N 渲染为可定位到对应依赖卡片的交互元素
    - 6.3: 让点击引用打开与该编号同源的预览图片，而非仅显示装饰性文本
    - 6.4: 展示缺失、不可上传和 mismatch 状态，避免生成假链接
    - 6.5: 增加前端测试或浏览器回归，验证编号、卡片、预览地址和媒体顺序一致

- [x] Task 7: 完成端到端审计与回归验证
    - 7.1: 输出 S01-S07 逐镜审计结果，覆盖模式、帧、参考图、API 输入、网页预览、动作、对白和结束状态
    - 7.2: 运行 schema、segments、produce、前端和现有项目测试
    - 7.3: 使用实际 ep01 数据验证合格镜头可生成请求、缺失镜头被正确阻断
    - 7.4: 核验 assetSummary 仅在资产和引用映射完整时变为 ready
    - 7.5: 记录 OneAPI 失败或外部生成服务不可用造成的剩余阻塞
    - 7.6: 生成 summary.md，记录已完成修正、验证结果和未解决的外部依赖
