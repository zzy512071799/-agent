# Wan3 情景内联图片引用修正总结

## 完成内容

- 扩展 `src/core/pipeline/wan3-media.ts` 的 `buildWan3ReferencePlan`：引用优先根据真实文件名主体和语义锚点插入 Prompt 正文，不再把所有引用默认追加到末尾。
- 锚点搜索排除“首帧锁定”“结束状态”“运镜”“声音描述”等说明段，避免角色引用被插入到非动作句。
- 对无法可靠定位的图片保留 `未定位参考约束`，不猜测插入位置。
- 已有显式 `@图片N` 继续按最终媒体数组规范化；连续尾帧已明确标注时不会重复平移。
- 更新《人间渡》EP01 S01/S02/S05 的必要语义：
  - S01 明确“状元桥街市”和“叶生”进入画面的动作节点。
  - S02 明确“状元桥街市”作为承接环境。
  - S05 明确仍在“堂屋内”。
- `/api/segments` 与 `/api/produce` 继续使用同一套引用编译结果，媒体数组顺序没有改变。

## 实际内联结果

- S01：街市、叶生、闻笙、旧药箱、阿离均位于对应出场或动作句。
- S02：闻笙、叶生、阿离、旧药箱、状元桥街市均位于视野、动作或环境承接句。
- S03：已有的闻笙、叶生、漏风书房引用保持在对应记忆和动作节点。
- S04：旧宅堂屋、叶生、妇人位于进门、出场和反应动作句。
- S05：旧宅堂屋、叶生、妇人位于堂屋承接、表情和指向动作句。
- S06：叶生和闻笙位于出门、墙影出现和主观视角动作句。
- S07：叶生、闻笙、铃位于回头、抽铃和道具状态动作句。

## 验证结果

- `npm run typecheck`：通过。
- `npm run build`：通过；保留 Next.js 动态文件系统 tracing 警告。
- `/api/segments?work=人间渡&episode=ep01`：S01-S07 所有图片 token 均能对应同一响应中的依赖卡片，未发现未定位引用。
- 页面端 media 顺序与 token 编号闭合检查：通过。
- Wan3 dry-run：S01 正常编排；S02-S07 因缺少上一镜真实尾帧而正确阻断，没有回退到虚假设计首帧。
- `assetSummary.status`：`ready`；静态图片 19 张，缺失引用 0，问题 0。
- `npm run lint`：当前仍无法执行，项目未安装 `eslint`，报错为 `eslint: command not found`。

## 生产前置条件

需要先生成并落盘 `media/videos/S01_last_actual.png`，才能开始 S02；后续每一镜也必须先生成真实尾帧，再进入下一镜。代码不会用设计首帧或不存在的文件伪造连续性引用。
