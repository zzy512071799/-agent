# Wan3 情景内联图片引用修正任务计划

- [✓] Task 1: 建立情景锚点与共享引用编译器
    - 1.1: 读取现有 `buildWan3ReferencePlan`、Prompt 引用校验和镜头元数据结构
    - 1.2: 设计角色、场景、道具与文件名到 Prompt 语义锚点的映射规则
    - 1.3: 扩展共享引用计划返回值，记录最终 refs、编译后 Prompt 和未定位引用
    - 1.4: 保持连续尾帧存在性判断、媒体顺序和 `@图片N` 编号规则不变
    - 1.5: 确保已有显式 `@图片N` 不被重复平移或重复注入

- [✓] Task 2: 将真实图片引用内联到 EP01 情景节点
    - 2.1: 为 S01-S07 建立角色、场景、道具的可靠语义锚点
    - 2.2: 将角色引用放入角色出场、观察、移动和接触动作句
    - 2.3: 将场景引用放入空间建立、镜头依赖和关键环境状态句
    - 2.4: 将道具引用放入拿起、背负、打开、抽出、使用和特写句
    - 2.5: 保持原有剧情、对白、动作因果链、镜头参数和结束状态不变
    - 2.6: 对无法安全定位的引用保留明确的末尾约束并记录原因

- [✓] Task 3: 统一 segments、produce 与网页预览映射
    - 3.1: 让 `/api/segments` 使用共享情景内联编译结果
    - 3.2: 让 `/api/produce` 使用同一编译结果提交 Prompt 和 media
    - 3.3: 确认每个 `@图片N` 仍对应 Wan3 media 中同序图片
    - 3.4: 确认网页 Prompt 引用、依赖卡片和预览地址保持一一对应
    - 3.5: 对缺失尾帧、缺失图片、mismatch 和超限引用保持阻断行为

- [✓] Task 4: 执行 EP01 端到端审计与回归验证
    - 4.1: 逐镜检查 S01-S07 的引用是否位于对应情景节点
    - 4.2: 验证有无真实尾帧两种情况下的 refs 顺序和 Prompt 编号
    - 4.3: 运行 `npm run typecheck` 和 `npm run build`
    - 4.4: 调用 `/api/segments` 检查 token、图片名和预览地址闭合
    - 4.5: 调用 Wan3 dry-run 检查可生成镜头与连续性阻断镜头
    - 4.6: 核对 `assetSummary.status` 和现有资产状态不回归
    - 4.7: 记录 lint 或外部生成服务不可用等环境阻塞

- [✓] Task 5: 生成最终修正总结
    - 5.1: 更新任务清单完成状态
    - 5.2: 记录代码修改、EP01 逐镜审计结果和验证命令
    - 5.3: 记录尚未具备真实尾帧时的生产前置条件
